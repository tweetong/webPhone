このプログラムは絵を書きながら会話ができるものである。

make allではserver.out client.outが生成され、client.outにcommand line引数でserver側のIPアドレスを与えることで動く。

make guiではお絵かき機能だけのgui_server.out gui_client.outが生成され、gui_server.outではcommand line引数に50001をgui_client.outではcommand line引数にserverのIPアドレスを入れる
